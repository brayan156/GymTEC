import { Time } from "@angular/common";

/**
 * Varaibales untilizadas para gestion datos de esta calse en el html
 */
export class Clase {
  imagen: string;
  horaFin: number ;
  horaInicio: number ;
  fecha: string;
  capacidad: number;
  id: number;
  idServicio: number;
  idEmpleado: number;
}
